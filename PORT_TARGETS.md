# Port targets — `itertools-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `itertools` that codex-rs uses, and therefore the minimum subset that `itertools-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-71] (rank `49`, tier `direct`, status `To Do`)

**Cargo package name**: `itertools`
**Rust module name (in `use` statements)**: `itertools`

## Summary

- **1** distinct symbols imported by codex-rs
- **4** total `use` statements
- **4** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `itertools::Itertools`

- **4 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`
- Aliases used: `as _`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/bottom_pane/list_selection_view.rs:4` (`codex-tui`) — `use itertools::Itertools as _`
- `tui/src/bottom_pane/title_setup.rs:1` (`codex-tui`) — `use itertools::Itertools`
- `tui/src/exec_cell/render.rs:22` (`codex-tui`) — `use itertools::Itertools`
- `tui/src/wrapping.rs:885` (`codex-tui`) — `use itertools::Itertools as _`
